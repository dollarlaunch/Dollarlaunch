<div class="container">
<<<<<<< HEAD
  <div class="row">
    <div class="col-md-12">
      <table class='table'>
        <thead>
          <tr>
            <th>Title</th>
            <th>Description</th>
            <th>User</th>
            <th>Show</th>
            <th>Edit</th>
            <th>Destroy</th>
          </tr>
        </thead>
        <tbody>
          <% @posts.each do |post| %>
            <tr>
              <td><%= post.title %></td>
              <td><%= post.description.truncate(20) %></td>
              <td><%= post.user.username %></td>
              <td><%= link_to "Show", post_path(post) %></td>
              <% if can? :update, post %>
                <td><%= link_to "Edit", edit_post_path(post) %></td>
              <% else %>
                <td><small class="mark">Access Denied</small></td>
              <% end %>
              <% if can? :destroy, post %>
                <td><%= link_to "Destroy", post_path(post), method: :delete, data: { confirm: "Are You Sure?" }%></td>
              <% else %>
                <td><small class="mark">Access Denied</small></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
      <hr>
=======
  <div class="row mt-4">
    <div class="col-md-10">
      <h4>Blog</h4>
    </div>
    <div class="col-md-2">
>>>>>>> e981ec2... Home Page and Blog PaGe Design, All Phases Completed and The Site is on Production, doing some optimzations
      <%= link_to "New Post", new_post_path, class:"btn btn-primary btn-sm" %>
    </div>
  </div>
  <hr>
  <div class="row">
    <% @posts.each do |post| %>
      <div class="col-md-4 mb-4">
        <div class="card">
          <%= image_tag(post.image.url, class:'post-img') %>
          <div class="card-body">
            <div class="mb-3">
            <small data-toggle="tooltip" data-placement="top" title="Writen By" class="text-muted"><%= post.user.username%></small>
            <small data-toggle="tooltip" data-placement="top" title="Published Date" class="ml-2 text-muted"><%= time_ago_in_words(post.created_at) %> ago</small>
            </div>
            <h5 class="card-title mb-3" style="color: #009e74;" ><%= post.title.truncate(30) %></h5>
            <p class="card-text"><%= post.description.truncate(45) %></p>
            <hr>
              <%= link_to "Read More", post_path(post), class:"btn btn-primary btn-sm" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>